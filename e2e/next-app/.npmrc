# Pin to the canonical registry. A global ~/.npmrc may point at a mirror
# (e.g. registry.npmmirror.com) that lags behind on freshly published
# prereleases, which breaks installs of the @meonode/ui beta this plugin
# is developed against.
registry=https://registry.npmjs.org
